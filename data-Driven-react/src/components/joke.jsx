export default function propsPracitce(prop) {
    console.log(prop)
    return (
        <>
            <h3>Joke: {prop.setup}</h3>
            <p>Punchline: {prop.punchline}</p>
        </>
    )
    
}